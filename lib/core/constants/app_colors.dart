import 'package:flutter/material.dart';

class AppColors {
  static const background = Color(0xFF0A0A1A);
  static const surface = Color(0xFF12122A);
  static const text = Color(0xFFE0E8FF);
  static const textMuted = Color(0xFF8890AA);
  static const accent = Color(0xFF7B5CF0);
  static const accentGlow = Color(0x557B5CF0);
  static const gold = Color(0xFFF0C040);
  static const success = Color(0xFF4CAF50);
  static const danger = Color(0xFFEF5350);

  static const borderGlow = [
    BoxShadow(color: Color(0x887B5CF0), blurRadius: 12, spreadRadius: 1),
    BoxShadow(color: Color(0x334488FF), blurRadius: 24, spreadRadius: 2),
  ];
}
